# TODO — 남은 폴리싱 (다음 세션 핸드오프)

> 이 파일은 장기 폴리싱 루프의 인수인계 문서입니다. 게임은 `DESIGN.md`/`CLAUDE.md`(v4, 포획형 몬스터 테이머 TD)를 정본으로 합니다.
> 각 항목: 무엇을 · 어디를(파일) · 어떻게. 작업 후 `tsc --noEmit` + `vite build` + preview 스크린샷으로 검증하고 커밋하세요.
> 빌드/실행: `npm run dev`(개발), `npm run build`. Node PATH는 `C:\Program Files\nodejs`.

## 남은 작업 (우선순위)

### 1. [HUD/레이아웃] 상단 우측 버튼 ↔ 기지 HP바 겹침 최종 정돈 (#34/#29)
- 증상: 전투 HUD 우측 상단 버튼(몬스터 보기/⌂ 나가기/속도 1·2·3x)이 스테이지에 따라 기지(성) 위 3D HP바와 겹쳐 보임.
- 위치: `src/ui/UI.ts` `buildHUD`(#hud-actions), `src/render/fallback.ts` `makeBase`(성 위 HP바 `makeBarSprite`), `src/render/Scene.ts`.
- 방향(택1):
  - (a) 성 위 3D HP바 제거(상단 HUD `#hp-bar` 게이지로 충분) → 겹침 원천 제거.
  - (b) `#hud-actions`를 좌측/하단 등 성이 오지 않는 위치로 이동, 또는 반투명 패널 배경으로 UI 영역 분리.
- 참고: 뷰어 열 때 HUD 숨김은 이미 처리됨(`openViewer`→`setGameplayVisible(false)`).

### 2. [사운드/이펙트 품질] 적당히 강화 (과하지 않게) (#35)
- 위치: `src/audio/Sfx.ts`(Web Audio 합성음), `src/render/VFX.ts`(파티클/링/플로팅텍스트).
- 아이디어: 카드 종류별 소리 살짝 차별화, 진화/승리 시 좀 더 풍부한 화음, 타격 이펙트에 색/스케일 변주. **너무 시끄럽지 않게** 볼륨/개수 절제.
- 설정 연동됨(`settings.sfx`/`volume`). 스로틀 이미 있음(평타 `Battle.atkSfxAt`).

### 3. [골드 사용처] 간단 상점 (#35)
- 현재 골드는 적 처치/이벤트로만 쌓이고 쓸 곳이 없음(`GameState.gold`).
- 방향: 스테이지 사이(로비 또는 갈림길 대체 노드)에 **상점** — 골드로 성 HP 회복 / 스탯 강화(5스탯 중 택1) / 카드 리롤 등 구매.
- 위치: `src/ui/UI.ts`(모달), `src/core/Game.ts`(플로우), `src/core/GameState.ts`(골드 차감·효과). `applyBuff`(5스탯 버프) 재사용 가능.
- 밸런스: 처치 골드(잡몹 3/미니 30/보스 100)와 가격 균형.

### 4. [맵 완성도] 추가 다듬기 (선택, #36)
- 경로 5종(`constants.ts` `PATH_LAYOUTS`) + 테마 장식(`Scene.buildDecor`)은 구현됨.
- 더: 테마별 배경색/안개, 경로 타일 텍스처 변주, 장식 밀도/종류 확대, 기지 주변 연출.

## 이번 대규모 세션에서 이미 완료된 것 (참고)
- 스테이지별 경로 5종 + 테마 맵 재구성(`setStageLayout`/`Scene.rebuildMap`), 테마 장식
- 스탯 5종(공/사거리/공속/치확/치피) + 레벨/진화 성장 + 전투 치명타 연출, 뷰어 스탯(이모지)
- 웨이브 보너스 = 5스탯 3택1(이모지), 포획 카드 다음웨이브 반영(웨이브 덱 스냅샷)
- 속성 컨셉: 물 마나보충/넉백, 어둠 공포(역주행), 풀 드로우·덩굴벽 블록, 빛 전체정지, 불 도트
- 도감 3D 감상(OrbitControls), 진화 연출 강화, 뷰어 HUD 겹침 수정, 로비→타이틀 복귀, 폰트(Gothic A1)
- 타이틀 수직 메뉴, 로비/전투버튼 하단중앙, 카드 속성 배경/테두리, 배치 반투명 모델 미리보기, 평타 사운드
- 야생 크리처 적(스테이지별 진화형/초반 제외/중복 흡수), 효과음/설정 모달, 마나 💧, 전투 ESC 나가기

## 작업 규칙
- 코드가 정본. 문서와 코드가 어긋나면 코드에 맞춰 문서 갱신.
- 매직넘버는 `src/data/constants.ts`, 카드는 `src/data/skills.csv`, 캐릭터는 `src/data/monsters.csv`.
- 협동기/분기진화/타락체 보스는 명시 요청 전 신규 도입 금지(v3 잔재).
